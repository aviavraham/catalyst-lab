apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: tempo
  namespace: catalystlab-shared
  labels:
    app: tempo
    release: prometheus-stack
spec:
  selector:
    matchLabels:
      app: tempo
  endpoints:
    - port: http
      interval: 15s
      path: /metrics
